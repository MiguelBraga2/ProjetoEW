extends layout

block content
    nav.navbar.navbar-expand-lg.navbar-light.bg-light
        .container-fluid
            a.navbar-brand(href='/') Acordãos
            button.navbar-toggler(type='button' data-bs-toggle='collapse' data-bs-target='#navbarCollapse')
                span.navbar-toggler-icon
            .collapse.navbar-collapse.justify-content-between#navbarCollapse
                .navbar-nav
                    a.nav-item.nav-link.active(href='/pesquisas') Pesquisas
                form.d-flex(action="/pesquisa")
                    .input-group
                        input.form-control(type='text' placeholder='Search' name='search')
                        button.btn.btn-secondary(type='submit')
                            i.fa-solid.fa-magnifying-glass.fa-rotate-90
                .navbar-navi.fa-solid.fa-magnifying-glass.fa-rotate-90 
                    a.nav-item.nav-link(href='/login') Login

    .container-fluid.mt-4.p-4 
        h2 Acordão
            button.btn 
                i.fa-regular.fa-star#fav-icon(style="color: #f0dd0a")
        table.table.table-striped.table-hover
            thead 
                th Campo 
                th Valor 
            tbody 
                each val in Object.entries(processo)  
                    // Devolve uma lista de arrays [[campo, valor], [campo, valor]]
                    tr 
                        td= val[0]
                        td= val[1]
    script. 
        // jQuery
        $(document).ready(function() {
        $('#fav-icon').hover(
            function() {
                $(this).attr("class","fa-star fa-solid");
            },
            function() {
                $(this).attr("class","fa-star fa-regular");
            }
        );
        });
